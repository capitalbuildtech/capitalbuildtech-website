export const metadata = {
  title: {
    template: '%s | Capital BuildTech Engineering',
    default: 'Projects',
  },
  description:
    'Browse the Capital BuildTech Engineering project portfolio structure with searchable, filterable project cards ready for verified project data.',
};

export default function ProjectsLayout({ children }) {
  return children;
}
